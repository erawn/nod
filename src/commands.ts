// import { nodState } from "./state";
// import { find } from '@lumino/algorithm';
// export function addButtons() {
//     const state = nodState.Instance()
//     const namesIterator = state.tracker.currentWidget?.toolbar?.children()

//     array.forEach(element => {

//     });
//     const existing = namesIterator
//         ? find(namesIterator, widget => widget.)
//         : false;
//     if (existing) {
//         return false;
//     }

//     state.tracker.currentWidget?.toolbar.insertBefore(
//         'spacer',
//         'exportButton',
//         ExportButtonComponent(notebookTracker, state)
//     );

//     state.app.deactivatePlugin
// }